name: Run WP Data Updates

on:
  workflow_dispatch:
    inputs:
      mode:
        description: "Run mode"
        required: true
        default: "once"
        type: choice
        options:
          - once
          - due
          - session
  push:
    branches:
      - main
    paths:
      - scripts/wp/run_wp_data_session.py
      - scripts/wp/trigger_wp_data.txt
      - .github/workflows/run_wp_data_10min.yml
  schedule:
    # GitHub cron is best-effort and can be delayed. A simple 5-minute
    # heartbeat gives it repeated chances to start; the lightweight gate below
    # maps a late start to the latest due business slot and deduplicates it.
    - cron: "2-57/5 1-3,5-7 * * 1-5"
    - cron: "57 4 * * 1-5"

permissions:
  contents: write

concurrency:
  group: wp-data-${{ github.ref }}
  cancel-in-progress: false

jobs:
  wp-data:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v5
      - name: Resolve latest due data slot
        id: due
        run: python scripts/wp/check_wp_data_due.py
        env:
          WP_DATA_RUN_MODE: ${{ github.event_name == 'schedule' && 'due' || github.event_name == 'push' && 'once' || github.event.inputs.mode || 'once' }}
          WP_SCHEDULE_GRACE_SECONDS: "600"
      - uses: actions/setup-python@v6
        if: steps.due.outputs.should_run == 'true'
        with:
          python-version: "3.11"
          cache: pip
      - name: Install dependencies
        if: steps.due.outputs.should_run == 'true'
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Run WP data update
        if: steps.due.outputs.should_run == 'true'
        run: python scripts/wp/run_wp_data_session.py
        env:
          TUSHARE_TOKEN: ${{ secrets.TUSHARE_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_EVENT_NAME: ${{ github.event_name }}
          ENABLE_AUCTION: "1"
          ENABLE_MINUTE: "1"
          REALTIME_MINUTE_ONLY: "1"
          ENABLE_MARKET_MINUTE_SCAN: "1"
          TRY_FULL_MARKET_MINUTE: "0"
          REALTIME_QUOTE_CHUNK_SIZE: "300"
          MINUTE_FREQ: "1min"
          MAX_MINUTE_SYMBOLS: "6000"
          WP_INTRADAY_MIN_PCT: "8"
          WP_DATA_RUN_MODE: ${{ github.event_name == 'schedule' && 'due' || github.event_name == 'push' && 'due' || github.event.inputs.mode || 'once' }}
          WP_SCHEDULE_GRACE_SECONDS: "600"
          WP_TRIGGER_REPO: "njedu2023-prog/WP"
          WP_TRIGGER_TOKEN: ${{ secrets.WP_TRIGGER_TOKEN || secrets.GH_PAT || secrets.PERSONAL_ACCESS_TOKEN }}
